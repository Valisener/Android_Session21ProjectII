package projectimdb2.acadgild.com.imdbproject.interfaces.responsehandler;
//interface that handles the response
public interface ResponseHandler {
    //interface that handles updating the activity with new information from the json files
    void updateActivity(String data,String type);
}
